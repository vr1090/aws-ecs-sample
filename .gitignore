# Terraform working directory and state
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Terraform override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform CLI config (can contain credentials)
.terraformrc
terraform.rc

# Optional local variable files (keep shared env/*.tfvars tracked)
terraform.tfvars
terraform.tfvars.json
*.auto.tfvars
*.auto.tfvars.json
*.tfvars.local

# Sensitive plan outputs
*.tfplan

# Shell / OS / editor noise
*.swp
*.swo
*~
.DS_Store
Thumbs.db
.vscode/
.idea/
